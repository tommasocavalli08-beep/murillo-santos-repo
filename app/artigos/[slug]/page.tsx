import { getArticle } from '@/lib/articles'; import { notFound } from 'next/navigation'; import type { Metadata } from 'next'; import { site } from '@/lib/site';
const base=process.env.NEXT_PUBLIC_SITE_URL||'https://drmurillosantos.com.br';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=await getArticle(slug);if(!a)return{};return{title:a.title,description:a.excerpt,alternates:{canonical:`/artigos/${slug}`},openGraph:{type:'article',title:a.title,description:a.excerpt,publishedTime:a.publishedAt,modifiedTime:a.updatedAt,authors:[site.fullName]}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await getArticle(slug);if(!a)notFound();const schema={'@context':'https://schema.org','@type':'Article',headline:a.title,description:a.excerpt,datePublished:a.publishedAt,dateModified:a.updatedAt,author:{'@type':'Person',name:site.fullName,url:`${base}/sobre`},publisher:{'@type':'Person',name:site.fullName},mainEntityOfPage:`${base}/artigos/${slug}`};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><article className="article-page container"><span className="kicker">{a.category}</span><h1>{a.title}</h1><p className="article-lead">{a.excerpt}</p><div className="article-meta">Por {site.fullName} · {new Date(a.publishedAt).toLocaleDateString('pt-BR')}</div><div className="article-body">{a.content.split('\n').map((p,i)=>p.trim()?<p key={i}>{p}</p>:null)}</div><aside className="medical-note">Este conteúdo é informativo e não substitui avaliação médica individual.</aside></article></>}
