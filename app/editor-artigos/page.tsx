'use client';
import { useState } from 'react';
export default function Editor(){const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus('Publicando…');const f=new FormData(e.currentTarget);const payload=Object.fromEntries(f.entries());const r=await fetch('/api/articles',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();setBusy(false);if(r.ok){setStatus(`Publicado: /artigos/${j.slug}`);e.currentTarget.reset()}else setStatus(j.error||'Erro ao publicar.')}return <div className="editor-shell"><div className="editor-card"><span className="kicker">Painel editorial</span><h1>Publicar novo artigo</h1><p>Esta página não aparece na navegação pública. Não possui autenticação, conforme solicitado.</p><form onSubmit={submit}><label>Título<input name="title" required/></label><label>Categoria<input name="category" defaultValue="Saúde da mulher"/></label><label>Resumo<textarea name="excerpt" rows={3} required/></label><label>Conteúdo<textarea name="content" rows={16} required placeholder="Escreva em parágrafos. Use uma linha em branco entre eles."/></label><label>Slug opcional<input name="slug" placeholder="gerado-automaticamente"/></label><button className="btn primary" disabled={busy}>{busy?'Publicando…':'Publicar artigo'}</button><div className="editor-status">{status}</div></form></div></div>}
